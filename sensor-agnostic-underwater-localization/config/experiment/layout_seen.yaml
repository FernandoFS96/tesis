# @package _global_
# ============================================================================
# Experiment: RANDOM task, seen-layout split (spiral). Reproduces the old
# converging setup (every layout seen in train; all-points targets).
#   python scripts/train/train_np_geometry.py experiment=layout_seen model=cnp
# Generate + preprocess first:
#   python data/generate.py dataset=random_spiral_shared
#   python data/process_data.py --data-root data/random_task/spiral_shared --mode legacy
# ============================================================================
defaults:
  - override /dataset: random_spiral_shared
  - override /data: layout_seen
