# Dataset identity -- see topology_spiral.yaml for the full note.
# `mode: shared` -> one trajectory ensemble reused across all position-sets.
task: random
method: spiral
mode: shared          # -> raw at data/random_task/spiral_shared/
