# Spatial ANP: the anp baseline + sensor-position-aware front end ENABLED.
# Everything else is inherited from anp.yaml; ablate via model.spatial.* flags
# (ablation ladder documented in _spatial.yaml).
defaults:
  - anp
  - _self_

name: spatial_anp
spatial:
  enabled: true
